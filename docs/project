职业规划助手（LangChain + Dify Self-host）实现文档 v1.0

0. 目标与范围

实现一个职业规划助手，提供：

上传职业介绍文件（PNG/JPG/PDF/DOCX）

用多模态大模型从图片/页面中抽取文字（OCR via VLM）

抽取文本写入知识库（Chroma）

上传简历（PDF/DOCX/PNG/JPG）

将“简历全文”固定注入系统上下文（system prompt / context）

基于知识库自然语言问答

支持按文档类型区分检索（resume 优先 / career_intro 优先 / 混合）

返回引用片段（citations）

前端：Dify self-host（Chat App + Workflow + HTTP Tool）
后端：FastAPI + LangChain（解析/OCR/入库/检索/生成）
向量库：Chroma（本地持久化）
模型：OpenAI / Claude，通过 Dify 模型选择决定（后端以“provider+model”参数调用对应适配器）

1. 总体架构
1.1 组件

Dify（self-host）（通过本地部署的已有Dify实现）

提供：聊天 UI、文件上传、模型选择、Workflow 编排

通过 HTTP Tool 调你的后端 API（ingest/chat）

后端服务（FastAPI）

/v1/ingest：接收文件 + doc_type + user_id，调用多模态 LLM 抽取文本，chunk+embedding，写入 Chroma

/v1/chat：根据 query + 策略，从 Chroma 检索，RAG 生成答案（或把简历固定上下文）

Chroma（本地持久化目录）

多租户隔离：用 metadata user_id + doc_type filter

1.2 数据流

上传文件 →（必要时按页渲染为图片）→ 多模态 LLM 抽取文本 → 清洗 → 切分 chunk → embeddings → 写入 Chroma
问答 query → embeddings → Chroma 检索 → 组装 prompt（含引用）→ LLM 输出答案 + citations

2. 关键设计决策
2.1 OCR 走纯多模态 LLM（VLM）

要求：支持 PNG/JPG/PDF/DOCX
实现策略：统一走“图片输入”，避免复杂的传统 OCR 和各种解析差异。

PNG/JPG：直接送 VLM

PDF：逐页渲染成图片（page images），每页送 VLM，再合并文本

DOCX：用 python-docx 提取文本后用 LLM 进行格式处理作为文本

2.2 模型选择（OpenAI / Claude）由 Dify 决定

Dify 侧允许用户/应用选择模型，后端需要支持接收：

provider: "openai" | "anthropic"

model: 例如 "gpt-4o-mini" / "gpt-4.1" / "claude-3.5-sonnet" 等（由 Dify 传入）

api_key：直接写在一个本地yaml里


3. 数据模型与元数据规范
3.1 文档元数据（写入每个 chunk）
{
  "user_id": "u123",
  "doc_id": "doc_abc",
  "doc_type": "resume | career_intro",
  "job_keywords": "xxx, xxx, xxx",
  "file_name": "xxx.pdf",
  "file_ext": "pdf",
  "page": 1,
  "chunk_id": 12,
  "created_at": "2026-02-17T10:00:00Z",
  "source": "upload",
  "tags": ["optional"]
}

3.2 文本规范

统一使用 UTF-8

清洗：

去掉明显重复行、页码、页眉页脚（可先不做复杂，后续迭代）

合并断行（中文/英文）

保留分段符号（便于 chunk）

4. API 设计（Dify 通过 HTTP Tool 调用）
4.1 文件入库：POST /v1/ingest

Content-Type: multipart/form-data

字段：

file: 上传文件（pdf/docx/png/jpg）

user_id: string（由 Dify 传，或从会话映射）

doc_type: "resume" | "career_intro"

tags: 可选（string，JSON 字符串或逗号分隔都可）

provider: "openai" | "anthropic"

model: string

resume_mode: "rag" | "pinned" | "hybrid"（可选，默认 "rag"）

返回：

{
  "status": "ok",
  "doc_id": "doc_abc",
  "doc_type": "resume",
  "pages": 2,
  "chunks": 38,
  "text_preview": "前200字…"
}

4.2 问答：POST /v1/chat

Content-Type: application/json

请求：

{
  "user_id": "u123",
  "query": "我更适合做产品还是数据分析？",
  "provider": "openai",
  "model": "gpt-4.1-mini",
  "top_k": 6,
  "retrieval_policy": "auto | resume_first | career_first | blended",
  "resume_mode": "rag | pinned | hybrid"
}


返回：

{
  "answer": "…",
  "citations": [
    {
      "doc_id": "doc_abc",
      "doc_type": "resume",
      "file_name": "resume.pdf",
      "page": 1,
      "chunk_id": 5,
      "snippet": "…"
    }
  ],
  "debug": {
    "retrieval_policy": "resume_first",
    "used_resume_pinned": true,
    "retrieved_chunks": 6
  }
}

5. 后端项目结构（建议，可直接生成）
career-assistant-backend/
  app/
    main.py
    core/
      config.py
      logging.py
    llm/
      router.py          # provider/model -> client
      vlm.py      # VLM OCR + chat
      chat.py
    ingest/
      service.py         # file -> images -> VLM OCR -> clean -> chunk -> embed -> chroma
      converters.py      # pdf/docx/image -> images
      cleaners.py
      schemas.py
    rag/
      service.py         # retrieval policy + resume pinned + answer generation
      prompts.py
      schemas.py
    store/
      chroma_store.py    # chroma init, add, query with filters
      resume_cache.py    # pinned resume summary store (sqlite/json)
  data/
    chroma/              # persist_directory
    uploads/             # raw uploads (optional)
    resume_cache/        # pinned data
  docker-compose.yml     # optional
  requirements.txt
  README.md

6. 关键实现细节（LangChain + Chroma）
6.1 Chroma 初始化

persist_directory=./data/chroma

collection：建议一个 collection，靠 metadata filter 区分用户与类型

embeddings：一个固定 embedding 模型（建议与你生成模型可独立）

你允许 OpenAI/Claude 二选一，但Embedding 固定使用OpenAI的模型

6.2 chunk 策略

中文：chunk_size_chars=800，overlap=120

使用 RecursiveCharacterTextSplitter（分隔符：\n\n, \n, 。, ；, ，）

6.3 多租户检索过滤

Chroma query 时增加 filter：

{"user_id": user_id}

以及（可选）{"doc_type": "resume"}

7. 多模态 OCR（VLM）统一接口
7.1 OCR Prompt（强约束）

给 VLM 的指令（system 或 user）可以查找网上的已有较好模板进行，大致类似于：

只输出抽取文本，不要解释

保留原有结构（标题/段落/列表）

对表格按行输出（或 Markdown 表格）

若某处看不清，输出 [UNK]

示例（给编程助手实现成常量模板）：

OCR_INSTRUCTION：

“请从图片中尽可能完整、忠实地提取所有可见文字。保持原有段落与列表结构。仅输出提取出的文字，不要添加任何解释或总结。表格请按行输出或用 Markdown 表格表示。无法辨认处用 [UNK] 标注。”

7.2 PDF → images

用 PyMuPDF (fitz)：

page.get_pixmap(dpi=200~250)（中文清晰度通常足够）

转 PNG bytes，送 VLM

7.3 DOCX → images

推荐路径（最稳）：

libreoffice --headless --convert-to pdf 转为 pdf

然后按 PDF pipeline 渲染为 images

如果你不想依赖 libreoffice，可先用 python-docx 提取文本（但不符合“同样用LLM提取”的字面要求）。本方案默认用 libreoffice。

8. 简历实现

8.1 直接提取简历中所有文字，作为上下文提供给大模型供处理

9. 检索策略（retrieval_policy）

实现一个简单路由器：

resume_first：

先检索 doc_type=resume top_k/2

再检索 doc_type=career_intro top_k/2

career_first：反过来

blended：

不区分 doc_type，直接 top_k

auto：

根据 query 关键词判断：

“我/我的/简历/经历/擅长/技能/适合” → resume_first

“岗位/职业/工作内容/发展路径/要求/JD” → career_first

其它 → blended

输出 citations 需要标注 doc_type。

10. Dify Self-host 配置（Workflow 级别可落地）

这一部分可以等到完成代码部分后再进行处理

11. 最小可用（MVP）验收标准

上传 PNG 简历 → 返回入库成功 + chunks 数

上传 PDF 职业介绍 → 返回入库成功

问：“基于我的简历，我最匹配职业介绍里的哪个方向？” → 能引用 resume + career_intro 的片段 